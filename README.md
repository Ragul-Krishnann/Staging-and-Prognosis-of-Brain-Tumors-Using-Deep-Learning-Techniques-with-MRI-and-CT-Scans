# NeuroVision AI Frontend

React + TypeScript client for scan upload, MRI analysis, Grad-CAM preview, and report presentation.

## Run

```bash
npm install
npm run dev
```

Set `VITE_API_BASE_URL` if the backend is not running on `http://127.0.0.1:8000`.
